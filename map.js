const numbers =[1, 2, 3, 4, 5,6]
let newArray=[];
for(let number of numbers){
    // console.log(Number)
    const sum = number+1;
    newArray.push(sum)
}
// console.log(newArray)


// ? map use kore
const list =[1,2,3,4,5,6];
const newArry = list.map(number => number+1);
// console.log(newArray)
